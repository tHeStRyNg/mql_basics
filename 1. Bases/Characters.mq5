
void OnInit() {
   // Char is often used to store characters, but can also be used to store numbers
   char a = "a";  // WRONG: To save a single character you have to use the single quotes '', "" these quotes are used to save multiple characters
   char b = 'a';
   
   Print(b);
   
   // We can change the value
   b = 'b';
   
   Print(b);
   
   // Let's test the code in the strategy tester
}