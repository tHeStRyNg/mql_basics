
void OnInit() {
   // Much like we use integer values, we use decimal values
   double a = 0.12;
   
   Print(a);
   
   a = 1.34;
   
   Print(a);
   
    // When declaring a float variable it is important to always put an f at the end.
    // If we don't put the f we are trying to assign a value that occupies more than what a float should occupy.
   float b = 0.32f;
   
   Print(b);
   
// Let's test the code in the strategy tester
}